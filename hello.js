console.log("helllo, world");
console.log("you are for that only")
var var1 = "test 000";
const var1 = "testass";
console.log(var1);

const closserFuc = {
  const cash ={};
  return (key,value) =>{
    if(cash[`${key}`]){
      return cash[`${key}`]
    }else{
      cash[`${key}`] = value;
    }
  }
  
}
